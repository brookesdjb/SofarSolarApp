import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.brookes.solar',
  appName: 'SolarApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
